apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/acm-workshop/jmeter-tests:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/acm-workshop/jmeter-tests:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: jmeter-tests
        chartPath: charts/jmeter-tests
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/acm-workshop/jmeter-tests"
          image.tag: "{{.DIGEST_HEX}}"
